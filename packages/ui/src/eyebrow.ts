/**
 * The small-caps label that sits above a screen's title. It lives in one place
 * so the Plan, Library, and Run shells cannot quietly drift apart
 * (UI_DESIGN.md §1.3, §4).
 */
export const EYEBROW_CLASS = "uppercase tracking-wide text-muted";
